# Linear module (built-in)

This folder defines the optional **linear** WTK module. After install, configuration lives in your project at `_wtk/linear/config.yaml`.

See [Linear module — artifact destinations](../../docs/reference/linear-module.md) for how skills use it and how to enable Linear MCP in your IDE.
