# n8n environment specification

> Generated from live instance data via MCP where available. Fill gaps manually when a tool is unavailable.

## Instance

- **Deployment:** self-hosted | n8n Cloud (limited support in this skill set)
- **Base URL:** 
- **n8n version:** 
- **Database:** SQLite | PostgreSQL | MySQL (and version if known)
- **Execution mode:** regular | queue
- **Timezone / locale (if configured):** 

## Runtime & infrastructure

- **Install method:** Docker Compose | Kubernetes | bare metal | other
- **Compose / chart reference (path or repo link, no secrets):** 
- **Worker processes:** (count, image tag, queue name if queue mode)
- **Redis / broker:** (if queue mode)
- **Reverse proxy / TLS:** 
- **Environment highlights (names only, no values):** e.g. `N8N_*`, `QUEUE_*`, `DB_*`

## Editions & licensing

- **Registered community / enterprise features visible in UI:** 
- **Source of truth for license:** (billing portal, env, etc. — no secrets)

## Community nodes & extensions

List each with **package name**, **version**, and **purpose** (one line each).

| Package | Version | Purpose |
|--------|---------|---------|
| | | |

## Built-in capabilities snapshot

Summarize what MCP (or manual inspection) reported:

- **Credential types in use (types only, not secrets):** 
- **Workflow count (approx):** 
- **Projects / folders (if used):** 
- **Webhook / public URL pattern (describe, no tokens):** 

## Observability

- **Log destination:** container stdout | file | external aggregator
- **Metrics:** Prometheus endpoint? health check path?
- **Backup strategy (high level):** 

## Constraints & policies

- **Allowed external domains / APIs:** 
- **Secrets management:** Vault | Docker secrets | plain env (discouraged) | other
- **Data residency / PII rules:** 

## MCP probe metadata

- **MCP server name(s) used:** 
- **Tools invoked (list):** 
- **Probe timestamp (UTC):** 
- **Gaps / could not verify:** 
