---
name: wtk-n8n-dev-story
description: >-
  Produces a structured markdown “workflow story” for self-hosted n8n — triggers, data contracts, node outline,
  credentials (types only), errors, ACs, and a manual test plan — ready to implement in the editor. Use when the
  user wants `wtk-n8n-dev-story` (or slash `/wtk-n8n-dev-story` on platforms that map skills that way) to define
  a workflow before building it in n8n.
---

Follow the instructions in `./workflow.md`.
