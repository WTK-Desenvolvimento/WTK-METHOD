description = "Executes the {{name}} tool from the WTK Method."
prompt = """
Execute the WTK '{{name}}' tool.

TOOL INSTRUCTIONS:
1. LOAD the tool file from {project-root}/{{wtkFolderName}}/{{path}}
2. READ its entire contents
3. FOLLOW every instruction precisely as specified

TOOL FILE: {project-root}/{{wtkFolderName}}/{{path}}
"""
